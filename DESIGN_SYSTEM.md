# Go2 Controller 界面设计系统

## 定位

实时机器人控制台：保持 Windows 原生浅色外观，主要通过布局、留白、字体层级和状态反馈提升可用性。

## 颜色原则

- 普通面板、输入框、表格和按钮使用 Windows 当前系统主题颜色。
- 红色仅用于急停、停止流程和删除等危险操作。
- 橙色仅用于“停止移动（保持武装）”。
- 状态不能只靠颜色表达，必须同时显示中文文本。

## 视觉规则

1. 标题、全局状态和红色急停固定在窗口顶部，任何页签下都可访问。
2. 全部主要功能统一进入一个工作区页签：“安全与姿态 / 运动控制 / 摄像头与识别 / 自动流程 / 错误日志”。同一时刻只展示一个任务，避免左右两套导航争抢空间。
3. 连接区始终显示连接状态和连接、断开操作；IP、AES key 等低频参数可折叠，连接成功后自动收起。
4. 姿态动作与互动动作分组；当前状态下无法执行的按钮必须禁用，不能让用户点击后才发现条件不满足。
5. 运动按钮保持统一尺寸；锁存移动时维持系统“按下”外观，停止后恢复。
6. 连接、摄像头和自动流程处理期间使用系统细进度条，不使用装饰性点动、闪烁或布局位移动画。
7. 进度反馈不阻塞输入、不延迟急停、不改变机器人通信时序。
8. 实时速度和日志使用等宽字体，防止数字变化造成跳动。
9. 识别刷新按钮直接显示当前快捷键，旁边使用只读下拉框选择 `F5–F12`；框色变化同时写入状态文字，不能只靠红/绿颜色表达。

## 实现约束

- 使用 Python 自带 `ttk.Style`，不强制切换主题，不增加第三方运行依赖。
- 所有机器人通信、安全门禁和自动流程逻辑保持不变。
- 默认窗口上限为 `1080×720`；页签内的单项功能可完整操作，小屏幕仍保留整页纵向滚动兜底。
- 窗口只有一个主工作区，不使用左右嵌套页签；最大化或缩放后重新计算页签高度与滚动边界。
- 内容小于视口时滚动位置固定为顶部，禁止出现顶部空白；窗口宽度变化不能产生横向滚动。
- 自动流程的步骤编辑区使用“动作 / 速度与时长”两行布局；编辑、文件和启动按钮均按可用宽度等分，禁止依赖固定单行宽度把控件挤出窗口。
